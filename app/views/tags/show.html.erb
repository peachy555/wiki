<h2>List of pages with tag: <%= @tag.name %></h2>

<ul>
  <% @tag.pages.each do |page| %>
    <li>
      <%= link_to page.name, page_path(page.id, topic_id: page.topic_id) %>
    </li>
  <% end %>
</ul>
