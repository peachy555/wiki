<h1>New Page!</h1>
<%= form_for @topic do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= hidden_field_tag 'topic_id', @topic_id %>
  <%= submit_tag "Go", class: "ui button primary" %>
<% end %>
